services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: spring-cgv-23rd-local:latest
    container_name: spring-cgv-app
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8080}:8080"
    env_file:
      - .env
    environment:
      TZ: Asia/Seoul
