global:
  scrape_interval: 15s # 15초에 한 번씩 매트릭 수집

scrape_configs:
  - job_name: 'server_metric'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: [ 'host.docker.internal:8080' ]  # 매트릭을 가져올 타겟
        labels:
          instance: 'prod_server'
