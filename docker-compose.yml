services:
  app:
    image: ${APP_IMAGE}
    container_name: spring-cgv-app
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8080}:8080"
    env_file:
      - .env
    environment:
      TZ: Asia/Seoul
